<UserControl
    x:Class="VkAudio.WPF.Views.FfmpegChoiceView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:VkAudio.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:VkAudio.WPF.ViewModels"
    Margin="16"
    d:DataContext="{d:DesignInstance vm:FfmpegChoiceViewModel, IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button
                Margin="0 0 0 16"
                HorizontalAlignment="Center"
                Command="{Binding DownloadCommand}"
                Content="Download" />
            <Button
                Command="{Binding SetPathsCommand}"
                Content="Set executables path"
                Style="{StaticResource MaterialDesignFlatButton}" />
        </StackPanel>
    </Grid>
</UserControl>
